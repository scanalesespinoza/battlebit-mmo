#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../license-reporter/bin/license-reporter" "$@"
  ret=$?
else 
  node  "$basedir/../license-reporter/bin/license-reporter" "$@"
  ret=$?
fi
exit $ret
